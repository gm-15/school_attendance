# 학교 출석 프로그램 개발 최종 견적서

## 📋 프로젝트 개요

**프로젝트명**: 학교 출석 프로그램 (School Attendance Program)  
**제출 기한**: 2025년 12월 16일  
**프로젝트 목표**: PDF 명세서 필수 조건 100% 충족 + 빈 디테일 자체 정의  
**작업 범위**: 백엔드(Node.js), 프론트엔드(React), DB(MySQL) 전 영역  
**팀 구성**: 2명  
**유지보수**: 기말 시험 후 유지보수 예정 없음 (코드 재사용 목적)

---

## 🛠 기술 스택 (최종 확정)

### 백엔드
- **언어**: Node.js
- **프레임워크**: Express
- **인증**: JWT + Refresh Token + HttpOnly Cookie (새로고침 로그인 유지 필수)
- **파일 업로드**: Multer
- **ORM**: Sequelize
- **추가 라이브러리**: 
  - dotenv (환경 변수)
  - cors (CORS 설정)
  - helmet (보안)
  - bcrypt (비밀번호 해싱)

### 프론트엔드
- **프레임워크**: React + Vite
- **상태 관리**: Context API
- **UI**: 단색(Gray-scale) 커스텀 CSS 테마
  - Chakra UI 제거 (교수 지침 "알록달록 금지" 반영)
  - Tailwind CSS minimal 사용 가능
- **아이콘**: Heroicons outline (최소 사용)
- **HTTP 클라이언트**: Axios
- **라우팅**: React Router
- **디자인**: 모바일 퍼스트 (Mobile First)

### 데이터베이스
- **DBMS**: MySQL
- **ORM**: Sequelize
- **제약조건**: 외래키, unique, CASCADE 처리

### 기타
- **파일 저장소**: 로컬 저장소
- **알림 시스템**: 폴링 방식 (추후 WebSocket 확장 여지)

---

## 📊 데이터베이스 스키마 설계 (최종 확정)

### 1. User (사용자)
- `id` (PK, INT, AUTO_INCREMENT)
- `role` (ENUM: 'Admin', 'Instructor', 'Student')
- `name` (VARCHAR)
- `email` (VARCHAR, UNIQUE, NOT NULL)
- `password_hash` (VARCHAR)
- `student_id` (VARCHAR, nullable, 학생인 경우)
- `refresh_token` (TEXT, nullable) - 쿠키 기반 로그인 유지용
- `department_id` (FK → Department)
- `created_at` (DATETIME)
- `updated_at` (DATETIME)

**결정 사항**:
- 학번 형식: 202321063 (입학년도4+학과2+순서3)
- 이메일: 중복 불허용 (UNIQUE)
- 비밀번호: 최소 6자, 대소문자+숫자+특수문자 필수
- refresh_token: HttpOnly Cookie에 저장

### 2. Department (학과)
- `id` (PK)
- `name` (VARCHAR, 학과명)
- `code` (VARCHAR, 학과 코드, 예: CS, EE)
- `created_at`, `updated_at`

### 3. Semester (학기)
- `id` (PK)
- `year` (INT, 연도)
- `term` (ENUM: '1', '2', '여름', '겨울')
- `start_date` (DATE)
- `end_date` (DATE)
- `created_at`, `updated_at`

### 4. Course (과목)
- `id` (PK)
- `title` (VARCHAR, 과목명)
- `code` (VARCHAR, 과목 코드, 예: CS101)
- `section` (INT, 분반, 1~4)
- `instructor_id` (FK → User)
- `semester_id` (FK → Semester)
- `department_id` (FK → Department)
- `room` (VARCHAR, 강의실)
- `created_at`, `updated_at`

**결정 사항**: 분반 최대 4개

### 5. ClassSession (수업 세션)
- `id` (PK)
- `course_id` (FK → Course)
- `week` (INT, 주차)
- `session_number` (INT, 회차)
- `start_at` (DATETIME)
- `end_at` (DATETIME)
- `room` (VARCHAR)
- `attendance_method` (ENUM: 'electronic', 'code', 'roll_call')
- `attendance_code` (VARCHAR(6), nullable, 6자리 인증번호)
- `status` (ENUM: 'scheduled', 'open', 'paused', 'closed')
- `attendance_duration` (INT, 출석 체크 가능 시간, 3~15분)
- `is_holiday` (BOOLEAN, 공휴일 여부)
- `is_makeup` (BOOLEAN, 보강일 여부)
- `created_at`, `updated_at`

**결정 사항**:
- 출석 방식: 전자출결, 인증번호, 호명 (3가지 모두)
- 인증번호: 6자리 숫자, 자동 생성
- 출석 체크 시간: 교수가 출석 시작 후 3~15분 설정 가능
- 공휴일: 네이버 달력 기준 (수동 입력)

### 6. Enrollment (수강신청)
- `id` (PK)
- `user_id` (FK → User)
- `course_id` (FK → Course)
- `role` (ENUM: 'student', 'ta')
- `enrolled_at` (DATETIME)
- `created_at`, `updated_at`

**결정 사항**:
- 엑셀 업로드 기능 (20점 중 5점 항목)
- 수강 취소 가능

### 7. Attendance (출석)
- `id` (PK)
- `session_id` (FK → ClassSession)
- `student_id` (FK → User)
- `status` (INT: 0=미정, 1=출석, 2=지각, 3=결석, 4=공결)
- `checked_at` (DATETIME, 출석 체크 시간)
- `late_minutes` (INT, 지각 시간, 분 단위)
- `location` (VARCHAR, nullable, 전자출결 시 위치 정보)
- `created_at`, `updated_at`

**결정 사항**:
- 지각 기준: 수업 시작 후 10분
- 지각→결석: 30분 이상

### 8. ExcuseRequest (공결 신청)
- `id` (PK)
- `session_id` (FK → ClassSession)
- `student_id` (FK → User)
- `reason_code` (ENUM: '병가', '경조사', '기타')
- `reason_text` (TEXT, 상세 사유)
- `status` (ENUM: 'pending', 'approved', 'rejected')
- `instructor_comment` (TEXT, nullable)
- `files` (JSON, 파일 경로 배열)
- `submitted_at` (DATETIME)
- `reviewed_at` (DATETIME, nullable)
- `created_at`, `updated_at`

**결정 사항**:
- 신청 기한: 하루 전 기본, 당일 이후는 교수 선택 가능
- 파일: 최대 1개, 사진/PDF/오피스/한글/ZIP

### 9. Appeal (이의제기)
- `id` (PK)
- `attendance_id` (FK → Attendance)
- `student_id` (FK → User)
- `message` (TEXT, 이의 내용)
- `status` (ENUM: 'pending', 'approved', 'rejected')
- `instructor_comment` (TEXT, nullable)
- `submitted_at` (DATETIME)
- `reviewed_at` (DATETIME, nullable)
- `created_at`, `updated_at`

**결정 사항**: 학기 종료 후 다음주 금요일까지

### 10. Announcement (공지사항)
- `id` (PK)
- `course_id` (FK → Course)
- `instructor_id` (FK → User)
- `title` (VARCHAR)
- `content` (TEXT)
- `is_pinned` (BOOLEAN, 고정 여부)
- `created_at`, `updated_at`

**결정 사항**: 작성자와 관리자 모두 수정/삭제 가능, 고정 기능 선택 가능

### 11. Message (메시지)
- `id` (PK)
- `course_id` (FK → Course)
- `sender_id` (FK → User)
- `receiver_id` (FK → User)
- `subject` (VARCHAR)
- `content` (TEXT)
- `is_read` (BOOLEAN, 읽음 여부)
- `created_at`, `updated_at`

**결정 사항**: 보낸 사람만 삭제 가능

### 12. Poll (투표)
- `id` (PK)
- `course_id` (FK → Course)
- `instructor_id` (FK → User)
- `title` (VARCHAR)
- `description` (TEXT)
- `session_id` (FK → ClassSession, nullable, 공강 대상 수업)
- `deadline` (DATETIME)
- `status` (ENUM: 'open', 'closed')
- `show_results_realtime` (BOOLEAN, 실시간 결과 공개 여부)
- `allow_revote` (BOOLEAN, 재투표 가능 여부)
- `created_at`, `updated_at`

### 13. PollVote (투표 참여)
- `id` (PK)
- `poll_id` (FK → Poll)
- `student_id` (FK → User)
- `vote` (ENUM: 'agree', 'disagree')
- `created_at`

**결정 사항**: 투표 생성 시 실시간/마감 후, 재투표 가능 여부 선택

### 14. AttendancePolicy (출석 정책)
- `id` (PK)
- `course_id` (FK → Course)
- `late_threshold` (INT, 지각 기준, 분, 기본값: 10)
- `late_to_absent_threshold` (INT, 지각→결석 전환 기준, 분, 기본값: 30)
- `max_absences` (INT, 최대 결석 허용 횟수)
- `absence_warning_threshold` (INT, 경고 결석 횟수, 기본값: 2)
- `absence_danger_threshold` (INT, 위험 결석 횟수, 기본값: 3)
- `absence_fail_threshold` (DECIMAL, 낙제 결석 비율, 기본값: 0.25)
- `attendance_weight` (DECIMAL, 출석 가중치, 기본값: 1.0)
- `late_weight` (DECIMAL, 지각 가중치, 기본값: 0.5)
- `created_at`, `updated_at`

**결정 사항**: 
- 과목별 정책
- 결석 25% 이상 시 낙제
- 출석=1, 지각=0.5

### 15. AuditLog (감사 로그) - 필수
- `id` (PK)
- `user_id` (FK → User, 수행한 사용자)
- `action` (VARCHAR, 작업 유형: 'attendance_change', 'approval', 'policy_change' 등)
- `target_type` (VARCHAR, 대상 타입: 'Attendance', 'ExcuseRequest' 등)
- `target_id` (INT, 대상 ID)
- `old_value` (JSON, nullable, 변경 전 값)
- `new_value` (JSON, nullable, 변경 후 값)
- `ip_address` (VARCHAR)
- `created_at` (DATETIME)

**결정 사항**: 
- 모든 민감 이벤트 로깅 필수 (출석 수정, 공결 승인/반려, 정책 변경)
- 로그 보관: 1년
- 관리자만 조회 가능

### 16. File (파일)
- `id` (PK)
- `original_name` (VARCHAR)
- `stored_path` (VARCHAR)
- `mime_type` (VARCHAR)
- `size` (BIGINT, 바이트)
- `uploader_id` (FK → User)
- `related_type` (VARCHAR, 관련 타입: 'excuse_request', 'appeal' 등)
- `related_id` (INT, 관련 ID)
- `created_at` (DATETIME)

**결정 사항**: 
- 파일 보관: 졸업 후 4년간
- 과제 파일: 업로더, 관련 교원, 관리자만 접근
- 게시판 파일: 해당 수업 수강생 모두 접근

### 17. Notification (알림)
- `id` (PK)
- `user_id` (FK → User, 수신자)
- `type` (ENUM: 'attendance_open', 'attendance_close', 'excuse_result', 'appeal_result', 'announcement', 'poll', 'absence_warning')
- `title` (VARCHAR)
- `content` (TEXT)
- `related_type` (VARCHAR, nullable)
- `related_id` (INT, nullable)
- `is_read` (BOOLEAN, 기본값: false)
- `created_at` (DATETIME)

---

## 🔌 API 엔드포인트 설계 (PDF 기준 100% 충족)

모든 API는 `/api` prefix 사용.

### 인증 (Authentication) - 필수
- `POST /api/auth/login` - 로그인 (JWT Access Token + Refresh Token Cookie)
- `POST /api/auth/refresh` - 토큰 갱신 (HttpOnly Cookie에서 refresh_token 읽어서 갱신)
- `POST /api/auth/logout` - 로그아웃 (Cookie 삭제)

**구현 요구사항**:
- Refresh Token은 HttpOnly Cookie에 저장
- 새로고침 시에도 로그인 유지 (PDF 필수 조건)
- Access Token은 메모리 또는 localStorage에 저장

### 사용자 관리 (User Management)
- `GET /api/users` - 사용자 목록 (관리자)
- `GET /api/users/{id}` - 사용자 상세
- `POST /api/users` - 사용자 등록 (관리자)
- `PUT /api/users/{id}` - 사용자 수정 (관리자)
- `DELETE /api/users/{id}` - 사용자 삭제 (관리자)
- `PUT /api/users/{id}/role` - 권한 변경 (관리자)

### 학과/학기/과목 관리
- `GET /api/departments` - 학과 목록
- `POST /api/departments` - 학과 생성 (관리자)
- `PUT /api/departments/{id}` - 학과 수정 (관리자)
- `DELETE /api/departments/{id}` - 학과 삭제 (관리자)

- `GET /api/semesters` - 학기 목록
- `POST /api/semesters` - 학기 생성 (관리자)
- `PUT /api/semesters/{id}` - 학기 수정 (관리자)
- `DELETE /api/semesters/{id}` - 학기 삭제 (관리자)

- `GET /api/courses` - 과목 목록
- `GET /api/courses/{id}` - 과목 상세
- `POST /api/courses` - 과목 생성 (관리자)
- `PUT /api/courses/{id}` - 과목 수정 (관리자)
- `DELETE /api/courses/{id}` - 과목 삭제 (관리자)

### 수업 세션 관리 (Class Session)
- `GET /api/courses/{id}/sessions` - 수업 세션 목록
- `POST /api/courses/{id}/sessions` - 수업 세션 생성 (교원)
- `PUT /api/sessions/{id}` - 수업 세션 수정 (교원)
- `DELETE /api/sessions/{id}` - 수업 세션 삭제 (교원)
- `POST /api/sessions/{id}/open` - 출석 열기 (교원)
- `POST /api/sessions/{id}/pause` - 출석 일시정지 (교원)
- `POST /api/sessions/{id}/close` - 출석 마감 (교원)
- `GET /api/sessions/{id}/attendance-code` - 인증번호 조회/생성 (교원)

**구현 요구사항**:
- 반복 규칙 기반 일정 생성
- 공휴일 수동 입력 처리
- 보강일 수동 추가

### 출석 관리 (Attendance)
- `POST /api/sessions/{id}/attend` - 출석 체크 (학생)
  - 인증번호 입력 또는 전자출결
- `GET /api/sessions/{id}/attendance` - 출석 현황 조회
- `GET /api/sessions/{id}/attendance/summary` - 출석 요약 (대시보드)
- `PATCH /api/attendance/{id}` - 출석 정정 (교원, 감사 로그 필수)
- `GET /api/courses/{id}/attendance` - 과목별 출석 현황

**구현 요구사항**:
- 출석 체크 시간: 교수가 출석 시작 후 3~15분 설정 가능
- 지각 자동 판정: 수업 시작 후 10분
- 지각→결석: 30분 이상

### 공결 관리 (Excuse Request)
- `POST /api/sessions/{id}/excuses` - 공결 신청 (학생, 파일 업로드 포함)
- `GET /api/excuses` - 공결 신청 목록 (교원: `?status=pending`)
- `GET /api/excuses/{id}` - 공결 신청 상세
- `PATCH /api/excuses/{id}` - 공결 승인/반려 (교원, 감사 로그 필수)

**구현 요구사항**:
- 신청 기한: 하루 전 기본, 당일 이후는 교수 선택 가능
- 사유 코드: 병가, 경조사, 기타
- 파일: 최대 1개, 사진/PDF/오피스/한글/ZIP

### 이의제기 (Appeal)
- `POST /api/attendance/{id}/appeals` - 이의제기 신청 (학생)
- `GET /api/appeals` - 이의제기 목록 (교원: `?status=pending`)
- `GET /api/appeals/{id}` - 이의제기 상세
- `PATCH /api/appeals/{id}` - 이의제기 처리 (교원, 감사 로그 필수)

**구현 요구사항**: 학기 종료 후 다음주 금요일까지

### 수강신청 (Enrollment)
- `GET /api/courses/{id}/enrollments` - 수강생 목록
- `POST /api/courses/{id}/enrollments` - 수강신청 (또는 관리자 등록)
- `DELETE /api/enrollments/{id}` - 수강 취소
- `POST /api/enrollments/import` - 엑셀/CSV 일괄 등록 (관리자, 20점 중 5점)

**구현 요구사항**: 엑셀 업로드 기능 필수

### 공지사항 (Announcement)
- `GET /api/courses/{id}/announcements` - 공지사항 목록
- `GET /api/announcements/{id}` - 공지사항 상세
- `POST /api/courses/{id}/announcements` - 공지사항 작성 (교원)
- `PUT /api/announcements/{id}` - 공지사항 수정 (교원)
- `DELETE /api/announcements/{id}` - 공지사항 삭제 (교원)

### 메시지 (Message)
- `GET /api/messages` - 메시지 목록 (받은/보낸)
- `GET /api/messages/{id}` - 메시지 상세
- `POST /api/messages` - 메시지 전송
- `PATCH /api/messages/{id}/read` - 읽음 처리
- `DELETE /api/messages/{id}` - 메시지 삭제

### 투표 (Poll)
- `GET /api/courses/{id}/polls` - 투표 목록
- `GET /api/polls/{id}` - 투표 상세
- `POST /api/courses/{id}/polls` - 투표 생성 (교원)
- `POST /api/polls/{id}/vote` - 투표 참여 (학생)
- `GET /api/polls/{id}/results` - 투표 결과 조회
- `POST /api/polls/{id}/close` - 투표 마감 (교원)

### 정책/점수 (Policy/Score)
- `GET /api/courses/{id}/policy` - 출석 정책 조회
- `PUT /api/courses/{id}/policy` - 출석 정책 설정 (교원, 감사 로그 필수)
- `GET /api/courses/{id}/score/attendance` - 출석 점수 조회

### 리포트/내보내기 (Report/Export) - 필수
- `GET /api/reports/attendance?course_id=...&week=...` - 출석 리포트
- `GET /api/reports/attendance/export` - 출석 리포트 엑셀 다운로드
- `GET /api/reports/risk-students` - 위험군 학생 목록

**필수 지표**:
- 출석률
- 지각→결석 전환 건수
- 공결 승인율
- 위험군 (2회·3회 경고)

### 파일 업로드 (File Upload)
- `POST /api/files` - 파일 업로드 (증빙 파일 등, Multer 사용)
- `GET /api/files/{id}` - 파일 다운로드
- `DELETE /api/files/{id}` - 파일 삭제

### 감사 로그 (Audit Log) - 필수
- `GET /api/audits?target_type=Attendance&target_id=...&user_id=...&start_date=...&end_date=...` - 감사 로그 조회 (관리자)

**구현 요구사항**: 모든 민감 이벤트 로깅 필수

### 대시보드 (Dashboard)
- `GET /api/dashboard` - 대시보드 데이터 (역할별로 다른 데이터 반환)
  - 학생: 내 출석 현황, 공결 신청 상태, 알림
  - 교원: 담당 수업 출석 현황, 승인 대기 건수, 알림
  - 관리자: 시스템 통계, 최근 활동

### 알림 (Notification)
- `GET /api/notifications` - 알림 목록 (폴링용)
- `PATCH /api/notifications/{id}/read` - 알림 읽음 처리

---

## 🎨 프론트엔드 페이지/컴포넌트 설계

### 디자인 원칙
- **단색 테마**: Gray-scale 커스텀 CSS
- **Chakra UI 제거**: 교수 지침 "알록달록 금지" 반영
- **모바일 퍼스트**: Mobile First 디자인
- **아이콘 최소화**: Heroicons outline만 사용

### 인증 관련
- `/login` - 로그인 페이지 (단색 테마)

### 대시보드
- `/dashboard` - 역할별 대시보드
  - 학생: 출석 현황 요약, 최근 알림, 공결 신청 상태
  - 교원: 담당 수업 목록, 승인 대기 건수, 최근 알림
  - 관리자: 시스템 통계, 최근 활동

### 관리자 페이지
- `/admin/departments` - 학과 관리
- `/admin/semesters` - 학기 관리
- `/admin/courses` - 과목 관리
- `/admin/users` - 사용자 관리
- `/admin/audit-logs` - 감사 로그 조회
- `/admin/reports` - 리포트 생성

### 교원 페이지
- `/instructor/courses` - 담당 과목 목록
- `/instructor/courses/{id}` - 과목 상세
- `/instructor/courses/{id}/sessions` - 수업 세션 관리
- `/instructor/courses/{id}/sessions/{sessionId}` - 세션 상세 (출석 현황)
- `/instructor/courses/{id}/sessions/{sessionId}/open` - 출석 열기 페이지
- `/instructor/courses/{id}/excuses` - 공결 신청 관리
- `/instructor/courses/{id}/appeals` - 이의제기 관리
- `/instructor/courses/{id}/announcements` - 공지사항 관리
- `/instructor/courses/{id}/polls` - 투표 관리
- `/instructor/messages` - 메시지 관리

### 학생 페이지
- `/student/courses` - 수강 과목 목록
- `/student/courses/{id}` - 과목 상세
- `/student/courses/{id}/attendance` - 출석 현황
- `/student/courses/{id}/sessions/{sessionId}/attend` - 출석 체크 페이지
- `/student/courses/{id}/excuses` - 공결 신청
- `/student/courses/{id}/appeals` - 이의제기
- `/student/messages` - 메시지

### 공통 페이지
- `/notifications` - 알림 목록
- `/profile` - 프로필 수정

---

## 📝 주요 기능별 작업 항목

### 1. 인증/인가 시스템 (필수)
- [ ] JWT Access Token 생성
- [ ] Refresh Token 생성 및 HttpOnly Cookie 저장
- [ ] `/api/auth/refresh` 엔드포인트 구현 (쿠키에서 refresh_token 읽기)
- [ ] 새로고침 시 자동 토큰 갱신 (Axios Interceptor)
- [ ] 역할 기반 접근 제어 (RBAC) 미들웨어
- [ ] 비밀번호 해싱 (bcrypt)

### 2. 사용자 관리
- [ ] 사용자 CRUD API
- [ ] 권한 관리
- [ ] 프로필 관리

### 3. 학과/학기/과목 관리
- [ ] 학과 CRUD API
- [ ] 학기 CRUD API
- [ ] 과목 CRUD API
- [ ] 관계 설정 및 검증 (Sequelize)

### 4. 수업 세션 관리
- [ ] 세션 CRUD API
- [ ] 반복 규칙 기반 일정 생성
- [ ] 공휴일 처리 (수동 입력)
- [ ] 보강일 처리
- [ ] 출석 방법 설정 (전자출결, 인증번호, 호명)
- [ ] 인증번호 자동 생성 (6자리)

### 5. 출석 관리
- [ ] 출석 체크 API
  - 인증번호 입력
  - 전자출결 (틀만, 나중에 구현)
  - 호명 (교원이 직접 체크)
- [ ] 출석 상태 자동 판정 (지각/결석)
- [ ] 출석 현황 조회 API
- [ ] 출석 정정 API (감사 로그 필수)
- [ ] 출석 요약 통계

### 6. 공결 관리
- [ ] 공결 신청 API (파일 업로드 포함)
- [ ] 공결 승인/반려 API (감사 로그 필수)
- [ ] 공결 신청 목록 조회
- [ ] 공결 상태 알림

### 7. 이의제기
- [ ] 이의제기 신청 API
- [ ] 이의제기 처리 API (감사 로그 필수)
- [ ] 출석 정정 시 감사 로그 기록

### 8. 알림 시스템
- [ ] 출석 오픈/마감 알림
- [ ] 공결 결과 알림
- [ ] 이의제기 결과 알림
- [ ] 강의 공지 알림
- [ ] 투표 알림
- [ ] 결석 경고 알림 (2회/3회)
- [ ] 알림 읽음 처리
- [ ] 폴링 방식 구현 (5-10초마다)

### 9. 커뮤니케이션
- [ ] 공지사항 CRUD API
- [ ] 메시지 전송/수신 API
- [ ] 메시지 읽음 처리

### 10. 투표 시스템
- [ ] 투표 생성 API
- [ ] 투표 참여 API
- [ ] 투표 결과 조회 API
- [ ] 투표 마감 API

### 11. 파일 관리
- [ ] 파일 업로드 (Multer)
- [ ] 파일 다운로드
- [ ] 파일 삭제
- [ ] MIME 타입 검증
- [ ] 파일 크기 제한

### 12. 리포트/통계 (필수)
- [ ] 출석률 통계
- [ ] 지각→결석 전환 건수
- [ ] 공결 승인율
- [ ] 위험군 학생 식별 (2회·3회 경고)
- [ ] 리포트 엑셀 다운로드

### 13. 감사 로그 (필수)
- [ ] 민감 이벤트 로깅 미들웨어
  - 출석 수정
  - 공결 승인/반려
  - 정책 변경
- [ ] 로그 조회 및 필터링 API
- [ ] 로그 보관 (1년)

### 14. 수강신청 (필수)
- [ ] 엑셀/CSV 일괄 등록 API (20점 중 5점)
- [ ] 수강 취소 API

### 15. 대시보드
- [ ] 역할별 대시보드 데이터 조회 API
- [ ] 실시간 출석 현황 표시
- [ ] 알림 요약

---

## 📅 작업 단계별 계획

### Phase 1: 프로젝트 설정 및 기본 구조 (1주차)
- [ ] Node.js + Express 프로젝트 초기 설정
- [ ] Sequelize 설정 및 MySQL 연결
- [ ] 데이터베이스 모델 생성 (17개 테이블)
- [ ] JWT + Refresh Token + HttpOnly Cookie 인증 시스템 구현
- [ ] CORS, helmet, dotenv 설정
- [ ] React + Vite 프로젝트 초기 설정
- [ ] 단색 CSS 테마 기본 설정
- [ ] 기본 라우팅 설정

### Phase 2: 핵심 기능 구현 - 백엔드 (2주차)
- [ ] 사용자 관리 API (CRUD)
- [ ] 학과/학기/과목 관리 API
- [ ] 수업 세션 관리 API
- [ ] 출석 관리 API (인증번호 방식 우선)
- [ ] 엑셀 수강신청 API (20점 중 5점)

### Phase 3: 핵심 기능 구현 - 프론트엔드 (2-3주차)
- [ ] 로그인 페이지 (단색 테마)
- [ ] 대시보드 (역할별, 단색 테마)
- [ ] 관리자 페이지 (사용자, 학과, 학기, 과목 관리)
- [ ] 교원 페이지 (수업 세션, 출석 관리)
- [ ] 학생 페이지 (출석 체크, 출석 현황)

### Phase 4: 고급 기능 구현 (3주차)
- [ ] 공결 신청/승인 API 및 UI (감사 로그 포함)
- [ ] 이의제기 API 및 UI (감사 로그 포함)
- [ ] 알림 시스템 (폴링 방식)
- [ ] 커뮤니케이션 (공지사항, 메시지)

### Phase 5: 추가 기능 구현 (4주차)
- [ ] 투표 시스템
- [ ] 리포트/통계 (필수 지표 포함)
- [ ] 감사 로그 조회
- [ ] 파일 관리

### Phase 6: 통합 및 테스트 (4-5주차)
- [ ] API 통합 테스트 (수동)
- [ ] 프론트엔드-백엔드 통합
- [ ] 사용자 시나리오 테스트
- [ ] 버그 수정

### Phase 7: 문서화 및 배포 준비 (5주차)
- [ ] README 작성
- [ ] API 문서 작성
- [ ] 다이어그램 작성 (ERD, 아키텍처, 코드 작동 방식)
- [ ] 코드 주석 추가
- [ ] 배포 설정 (로컬 네트워크 접근 가능하도록)

### Phase 8: 선택 기능 (시간이 남으면)
- [ ] 전자출결 기능 완성
- [ ] UI/UX 개선

---

## ✅ 최종 결정 사항 요약

### 기술 스택
- **백엔드**: Node.js + Express
- **프론트엔드**: React + Vite
- **상태 관리**: Context API
- **UI**: 단색(Gray-scale) 커스텀 CSS (Chakra UI 제거)
- **데이터베이스**: MySQL + Sequelize
- **인증**: JWT + Refresh Token + HttpOnly Cookie (새로고침 로그인 유지 필수)
- **파일 저장소**: 로컬 저장소
- **알림 시스템**: 폴링 방식

### 필수 구현 사항 (PDF 요구사항)
- ✅ 출석 방식 2가지 이상 (전자출결+인증번호+호명)
- ✅ 쿠키 기반 로그인 유지 (Refresh Token + HttpOnly Cookie)
- ✅ 엑셀 수강신청 (20점 중 5점)
- ✅ 감사 로그 (모든 민감 이벤트)
- ✅ 공결·이의제기 워크플로
- ✅ 리포트 (출석률, 지각→결석 전환, 공결 승인율, 위험군)
- ✅ 단색 UI (알록달록 금지)

### 출석 관련
- 인증번호: 6자리 숫자
- 지각 기준: 수업 시작 후 10분
- 지각→결석: 30분 이상
- 출석 체크 시간: 교수가 출석 시작 후 3~15분 설정 가능
- 출석 방법: 인증번호 자동 출석 + 호명 가능

### 공결/이의제기
- 공결: 하루 전 기본, 당일 이후는 교수 선택
- 사유: 병가, 경조사, 기타
- 파일: 최대 1개, 사진/PDF/오피스/한글/ZIP
- 이의제기: 학기 종료 후 다음주 금요일까지

### 출석 정책
- 과목별 정책
- 결석 25% 이상 시 낙제
- 출석=1, 지각=0.5

### 데이터
- 학번: 202321063 형식 (입학년도4+학과2+순서3)
- 이메일: 중복 불허용
- 비밀번호: 최소 6자, 대소문자+숫자+특수문자
- 분반: 최대 4분반
- 감사 로그 보관: 1년
- 파일 보관: 졸업 후 4년간

### 문서화
- **필수 문서**: README.md, API 문서, DB 스키마 문서, 사용자 매뉴얼
- **다이어그램**: ERD, 아키텍처, 코드 작동 방식 3개
- **코드 주석**: 간단하게 (무엇을 하는지 정도만)

---

**작성일**: 2024년 11월  
**작성자**: 프로젝트 팀  
**버전**: 3.0 (최종 확정 - Node.js 기반)

